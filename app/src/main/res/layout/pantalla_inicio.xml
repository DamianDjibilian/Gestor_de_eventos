<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fondo"
    tools:context=".PantallaInicio"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="10dp"
        >

        <TextView
            android:id="@+id/txtEventosCargados"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Eventos cargados"
            android:layout_marginTop="4dp"
            android:textSize="25dp"
            android:textColor="@color/timber">
        </TextView>

        <Button
            android:id="@+id/btnCargar"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_toEndOf="@id/txtEventosCargados"
            android:layout_marginLeft="100dp"
            android:background="@drawable/botonredondo"
            android:text="+"
            android:textColor="#FF000000"
            android:textSize="30dp"
            android:textStyle="bold"
            android:layout_toRightOf="@id/txtEventosCargados" />

    </RelativeLayout>
    <ScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:textSize="20dp"
        android:fontFamily="serif"
        android:textColor="@color/timber"
        >
        <ListView
            android:id="@+id/listaEventos"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >
        </ListView>


    </ScrollView>

</LinearLayout>